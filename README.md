# WHOAMI

Simple personal landing page with social profile links.

## Update social links

Edit `index.html` and replace the `href` values in the list items.

## Deploy

Pushing to `main` triggers `.github/workflows/deploy.yml`, which deploys the site to GitHub Pages.
